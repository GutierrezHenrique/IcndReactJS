import styled from "styled-components";

export const Button = styled.div`
    background: var(--blueGo);
    padding: 0rem 4rem;
    border-radius: .3rem;
    color: var(--white);
    transition: .2s;
    text-align: center;
    :hover {
        zoom: 1.1;
    }
`;

